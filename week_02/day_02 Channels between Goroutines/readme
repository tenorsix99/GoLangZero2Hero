; 🎯 เป้าหมายวันนี้:
; - เข้าใจว่า Channel คืออะไร
; - ใช้งาน chan เพื่อส่งข้อมูลระหว่าง Goroutine
; - ปิด Channel อย่างถูกต้อง (close)
; - ใช้ Channel แบบรับค่าเท่านั้น (receive-only) และส่งเท่านั้น (send-only)

